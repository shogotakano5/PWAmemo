# ---------------------------------------------------------------------------
# Every variable below is OPTIONAL. Without any of them the app still works
# fully — it just runs in local-only mode (memos are stored in the browser via
# IndexedDB) and account sign-up / the admin panel are disabled with a clear
# message in the UI.
#
# Never commit this file with real values filled in — copy it to .env.local
# (already gitignored) instead.
# ---------------------------------------------------------------------------

# Postgres connection string. Enables accounts + server-side memo storage.
# Works with Vercel Postgres / Neon / Supabase / any Postgres 12+.
# Neon & Supabase require sslmode=require.
#
# The app also accepts DATABASE_URL, POSTGRES_PRISMA_URL, or
# POSTGRES_URL_NON_POOLING for this — useful if you paste the block Vercel's
# Neon integration gives you as-is. Set only one; POSTGRES_URL wins if several
# are present.
POSTGRES_URL="postgres://user:password@host/dbname?sslmode=require"

# Secret used to sign session cookies AND to derive the keys that encrypt the
# stored e-mail addresses. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# Do NOT change this once accounts exist: the e-mail encryption key and the
# lookup index are derived from it, so changing it locks existing users out.
AUTH_SECRET=""

# ---------------------------------------------------------------------------
# SMTP (below) — OPTIONAL, but shared by two separate features:
#
#   1. Account self-service (forgot password/PIN, change e-mail) — enabled by
#      SMTP_HOST/SMTP_FROM alone (plus POSTGRES_URL/AUTH_SECRET above).
#   2. Admin panel (/admin) — additionally needs ADMIN_EMAILS below. Lets a
#      listed admin sign in with a one-time e-mail code to view and delete
#      accounts. "Admin" is just an address on this list — it does not need
#      its own memo account.
#
# Either feature is missing from the UI until its own requirements are met.
# ---------------------------------------------------------------------------

# Comma-separated allowlist of admin e-mail addresses. Only needed for the
# admin panel — leave unset to use SMTP for account self-service alone.
ADMIN_EMAILS="you@example.com"

# SMTP server used to send one-time codes. Works with any provider that
# exposes SMTP credentials (a mailbox, SendGrid, Mailgun, Amazon SES, ...).
# A bare hostname — not a URL. If your provider's dashboard shows the setting
# as "smtp://host:port", a leading smtp(s):// and trailing :port are stripped
# automatically, but a plain hostname (as below) is what SMTP_HOST expects.
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="username"
SMTP_PASS="password"
# The From: address on the login-code e-mail.
SMTP_FROM="メモアプリ <no-reply@example.com>"
# Set to "true" only if your provider requires implicit TLS on a port other
# than 465 (465 already defaults to secure).
SMTP_SECURE=""
